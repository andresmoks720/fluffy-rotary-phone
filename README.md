# audio-modem

Browser-based, half-duplex audio data transfer MVP.

## Quick start

```bash
pnpm install
pnpm test
pnpm typecheck
```

## Current status

- Protocol and product contract docs live under `docs/`.
- T0 contract constants and CRC32C baseline are implemented.
- See `docs/todo.md` for current implementation sequence.
