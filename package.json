{
  "name": "audio-modem",
  "private": true,
  "version": "0.1.0",
  "description": "Browser-based half-duplex audio modem MVP",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "verify:fast": "./tools/verify_fast.sh",
    "verify:protocol": "./tools/verify_protocol.sh",
    "verify:runtime": "./tools/verify_runtime.sh",
    "verify:mvp": "./tools/verify_mvp.sh"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "typescript": "^5.9.2",
    "vite": "^8.0.0",
    "vitest": "^2.1.1"
  }
}
